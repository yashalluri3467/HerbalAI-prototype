apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  # LoadBalancer is exposed on localhost by Docker Desktop, giving a stable
  # http://localhost URL that survives beyond a single port-forward session.
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
