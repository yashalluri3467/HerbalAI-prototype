fastapi
uvicorn[standard]
numpy
pydantic
pillow
python-multipart
requests
python-dotenv
opencv-python-headless
# CPU-only TensorFlow wheel: smaller, no CUDA stubs, correct for render's CPU free tier.
tensorflow-cpu
# NOTE: matplotlib is a TRAINING-ONLY dependency (utils/train_*). It is intentionally
# excluded from the runtime image; install it separately when retraining models.

# Session history persistence (Neon Postgres)
sqlalchemy[asyncio]
asyncpg
